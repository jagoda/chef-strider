---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[mongodb::10gen_repo]
      - recipe[mongodb::default]
      - recipe[strider::default]
    attributes:
